package com.coreJava.innerclass;

public class innerClass {
	/**Q) what is the diff between nested class and inner class?
	 * 			nested class is a part inner class , a non-static inner class is known as nested inner class(we can define class inside class is known as nested Inner class)			
	 * 
	 * 
	 * 1)static inner class:-
	 * 		a static class created inside a class is called as static inner class.
	 * 
	 * 2)Anonymous inner class:-
	 * 		a class creating for implementation of interface and extending a class is known as Anonymous inner class
	 * 
	 * 3)local inner class :-
	 * 		a class created inside a method is called local inner class
	 * 
	 * 4)Member inner class:-
	 * 		a class created inside a class and outside a method is known as Member inner class. 
	 * 
	 * Nested interface:-
	 * 		a interface created inside a class or interface.
	 */
	public static void main(String[] args) {
		

	}

}
